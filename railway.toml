[build]
builder = "NIXPACKS"
buildCommand = "cd packages/server && npx prisma generate && npx prisma db push && (npx tsc --skipLibCheck || true)"

[deploy]
startCommand = "cd packages/server && node dist/app.js"
healthcheckPath = "/api/health"
healthcheckTimeout = 100
